<UserControl x:Class="UI.Views.TransmitTask"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" MaxWidth="2px"/>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="1*" MaxWidth="2px"/>
        </Grid.ColumnDefinitions>


        <Grid Row="0" Column="0" ColumnSpan="3" x:Name="ProcessBarBorder">
            <Grid Background="Blue" HorizontalAlignment="Left" x:Name="ProcessBar"></Grid>
        </Grid>

        <Grid Row="0" Column="1">
            <Label x:Name="LabelFileName"/>
        </Grid>

    </Grid>
</UserControl>
